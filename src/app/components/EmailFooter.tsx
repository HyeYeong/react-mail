import { Heading } from '@chakra-ui/react';

type MailHeading = {
  // title: string | JSX.Element;
};

export const EmailFooter = ({  }: MailHeading): JSX.Element => (
  <p>
    this is footer
  </p>
);
